// Package lib is a dummy go package to ensure the lib folder containing our
// .so file isn't pruned by parts of the Go toolchain that a automatically
// remove any folders without any go code in them.

package lib
